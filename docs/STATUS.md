# STATUS — текущее состояние проекта

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-08-24 (этап 9 закрыт)
**Текущий этап**: Этап 10 — Деплой (☐ не начат)
**Следующий шаг**: взять этап 10 по навыку `/okdeploy`. Бо́льшая часть этапа
уже сделана попутно (сервер, CI, автовыкат, миграции, `/health`, ротация
логов, бэкапы), поэтому этап — это в основном сверка каждого пункта и то, что
осталось: проверенный откат на предыдущую версию, полный голосовой круг в 12
секунд на канале сервера и передача клиенту исходников и доступа к репозиторию.
Критерии приёмки в [`07_ROADMAP.md`](07_ROADMAP.md#этап-10--деплой).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас | ✅ | `stage-0-done` | `1c4e4fe` | 2026-08-14 |
| 1 | Голосовой круг | ✅ | `stage-1-done` | `95067b1` | 2026-08-14 |
| 2 | Текст и подсказки | ✅ | `stage-2-done` | `1cd2f61` | 2026-08-14 |
| 3 | Оценка произношения | ✅ | `stage-3-done` | `f488bf5` | 2026-08-15 |
| 4 | Лимиты | ✅ | `stage-4-done` | `fc70a4b` | 2026-08-15 |
| 5 | Подписка и оплата | ✅ | `stage-5-done` | `723d765` | 2026-08-22 |
| 6 | Настройки | ✅ | `stage-6-done` | `f1ef55e` | 2026-08-22 |
| 7 | Прогресс | ✅ | `stage-7-done` | `5736c50` | 2026-08-23 |
| 8 | Админка | ✅ | `stage-8-done` | `2df0850` | 2026-08-24 |
| 9 | Полировка | ✅ | `stage-9-done` | `69fd94f` | 2026-08-24 |
| 10 | Деплой | ☐ | `stage-10-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

Этап 9 закрыт 24.08.2026, этап 10 не начат. Свободных хвостов нет.

Что осталось на человека, не кодом:

- **Расхождение в цене.** В админке у обоих тарифов 539 ₽, а офферы lava.top
  списывают 549 ₽. Чинится в кабинете lava.top либо возвратом цены в админке.
- **Раздела «Помощь» нет ни в одном этапе.** Он есть в навигационной карте
  (`02_NAVIGATION.md`, уровень 2), но ни в ТЗ, ни в дорожной карте его нет —
  то есть по плану он не выйдет никогда. Решение о нём за клиентом.
- **Один vCPU — потолок нагрузки.** Двадцать одновременных кругов упираются в
  ffmpeg на единственном ядре: одиночный круг 2.5 с, худший из двадцати 11.1 с.
  В бюджет 12 секунд укладывается, запаса нет. С 24.08 это не оценка на глаз, а
  метрика: `/admin` → Нагрузка показывает приговор «запас есть / плотно /
  потолок», а при «потолке» бот пишет админам сам, раз в сутки. Подробности —
  [`08_DEPLOY.md`](08_DEPLOY.md#нагрузка-когда-менять-сервер).

- **Копии базы уезжают в S3 Beget** (24.08). Каждую ночь в 03:30 МСК, хранение
  7 дней + 4 воскресенья, уборка старого автоматическая и только внутри своего
  префикса — бакет общий с чужим проектом. Ключи в `dostupi.txt`.
  Восстановление — `python -m app.tools.s3backup восстановить <файл> --да`.
  В копию не входит `.env`: его при переезде кладут руками из `dostupi.txt`.

  Проверено на боевом 24.08 сразу после выката: копия снялась за 0.99 с
  (дамп 113 КБ, архив 21 КБ), скачалась обратно, архив целый — внутри 13
  таблиц, данные и версия миграции `0010_service_calls`. Расчёт нагрузки
  проверен на боевой базе подставными событиями (потом удалены): перцентили
  и приговор считаются, настоящих `round_timing` пока ноль — они появятся с
  первым же кругом после выката.

## Известные блокеры

Блокеров нет.

**Аккаунт SpeechSuper теперь платный.** Пробный ключ истёк 22.08.2026, сервис
сутки отвечал `{"error": "invalid expired", "errId": 41030}` при http 200 —
срок аккаунта, а не наша поломка (проверялось с двух разных сетей). 23.08
клиент оформил в кабинете `console.speechsuper.com` подписку Starter, $20/мес
через PayPal: 23 кредита, все языки и все типы API. Ключи менять не пришлось,
`SPEECHSUPER_APP_KEY` и `SPEECHSUPER_SECRET_KEY` остались прежними. Проверено
после оплаты и локально, и на боевом воркере: ответ 200 за 664 мс с разбором.

Про цены: у SpeechSuper минимум $20/мес в любом варианте, включая Pay As You
Go (там же квартальные счета и переписка с менеджером), поэтому подписка из
кабинета — самый дешёвый и быстрый путь. Кредиты докупаются пакетами только
поверх активной подписки.

Живость сервиса проверяется без бота — одним вызовом
`SpeechSuperPronunciation.assess` с любым WAV 16 кГц моно 16 бит; на боевом
это `docker compose exec -e PYTHONPATH=/app worker python …` (без `PYTHONPATH`
модуль `app` не найдётся, скрипт запускается не из рабочей директории).

**К сведению на будущее.** У СБП не бывает автосписаний, поэтому доступ по
нему продаётся разово, а перед окончанием уходит напоминание (ежедневная
задача `remind_expiring`, отметка `subscriptions.reminded_at`). Продавец не
может оплатить собственный продукт с почты своего аккаунта lava.top: платёжка
отвечает `Incorrect email to purchase`, бот в этом случае просит другой адрес.
Для проверок годится алиас того же ящика вида `имя+bot@gmail.com`.

**В базе разработки лежат тестовые данные оплаты** (`test-contract-0001` и
подписка по нему). На боевой базе данные настоящие, платежи живые, трогать их
не надо.

## История закрытий

Записывать в обратном хронологическом порядке. Формат:
`YYYY-MM-DD — Этап N: <название> — tag stage-N-done — короткая заметка с конкретикой верификации`.

**2026-08-24 — Этап 9: Полировка — tag `stage-9-done`** (коммит `69fd94f`).

Проверка разделилась надвое, и это записано как есть.

Машинно, с доказательствами: недоступность всех пяти внешних сервисов —
`tests/test_resilience.py` рвёт транспорт у каждого, каждый отдаёт опознаваемую
ошибку, юзер получает текст; 20 одновременных диалогов — двадцать настоящих
кругов на боевом, медиана 9.7 с, худший 11.1 с, сбоев ноль при одиночном круге
2.5 с (упирается в единственный vCPU на ffmpeg, стоило $0.014, тестовые юзеры
удалены); перезапуск очереди — отложенная задача пережила перезапуск redis и
воркера и выполнилась; секреты в логах — все девять реальных ключей с сервера
искались в 2062 строках логов, ноль совпадений; 260 тестов и чистый ruff.

Человеком в @ChineseToneBot — часть сценариев; остальные («прошёлся там, где
мог, остальное ок») приняты без живой проверки. Не подтверждены вживую: чужие
форматы, голосовое длиннее двух минут, тишина в записи, два голосовых подряд,
двойное нажатие платной кнопки. Код на всё это есть и покрыт тестами, но
живого подтверждения у этих пяти пунктов нет — если что-то всплывёт в руках
клиента, искать надо здесь.

Найдено и закрыто по дороге, сверх критериев этапа: **токен бота утекал в лог
из трейсбека** — маскировка стояла выше отрисовки исключения, и хватило бы 404
от файлового сервера Telegram (httpx кладёт в текст ошибки полный адрес
запроса); тест на регрессию падает со старым порядком процессоров. Два
голосовых подряд запускали два круга и оплачивались дважды на всех четырёх
сервисах — поставлен замок круга. Сетевой сбой провайдера теперь опознаётся
как сбой провайдера, отчего запасная озвучка включается и при обрыве связи, а
не только при пятисотке. Выкат больше не обрывает круг на середине
(`job_completion_wait` у arq плюс `stop_grace_period` у docker).

**2026-08-24 — Этап 8: Админка — tag `stage-8-done`** (коммит `2df0850`).

Живая проверка человеком в @ChineseToneBot, подтверждена событиями боевой базы.
Прошло всё восемь критериев: лимит разборов подняли с 3 до 4 и он лёг в
`settings`; цену обоих тарифов сдвинули с 549 на 539 ₽; две рассылки — на
сегмент «без подписки» и на «платящих» — доставлены 3 из 3 каждая, без ошибок
и блокировок, обе уложились в секунду и бот в это время отвечал; админа выдали
командой и тут же отобрали (`admin_added` → `admin_removed`); расход по
сервисам показал живые строки (`fish`, `openrouter`).

Что стоит помнить:

* Цена в админке — только витрина. Списывает платёжка по своему офферу, и после
  правки цены оффер надо править в кабинете lava.top. Экран об этом
  предупреждает, но само расхождение бот не чинит и починить не может.
* Расход — оценка по прайсам провайдеров, а не счёт. Прайсы меняются без
  предупреждения, поэтому лежат одним блоком в `app/core/usage.py`.
* `/admin` чужому не отвечает ничего, попытка пишется в события (`admin_denied`)
  и в лог. Любой ответ, даже отказ, подтверждал бы, что админка есть.
* Правка лимита действует со следующей проверки, потому что `get_limits` читает
  базу без кэша на каждом сообщении. Кэш здесь сэкономил бы миллисекунды и
  сломал бы главное обещание этапа.
* Рассылка держит 20 сообщений в секунду при разрешённых телеграмом 30: остаток
  оставлен живым разговорам, они идут через того же бота и в ту же квоту.
* Журнал расхода переживает откат сессии, но не переживает падение процесса:
  он живёт в памяти до ближайшего коммита. Терять единицы записей о центах
  дешевле, чем платить лишним запросом в каждом голосовом круге.

**2026-08-23 — Этап 7: Прогресс — tag `stage-7-done`** (коммит `5736c50`).

Живая проверка человеком в @ChineseToneBot, подтверждена боевой базой: серия
выросла с 1 до 2 дней (22 и 23.08), календарь закрасил ровно эти дни, счётчики
сошлись с базой (8 сообщений, 3 разбора), после починки SpeechSuper появился
средний балл 38 по трём попыткам. У тех, кто ещё не говорил, раздел показывает
приглашение, а не нули.

Решения, которые стоит помнить:

- Серия не рвётся, пока идёт сегодняшний день: занимался вчера, зашёл утром —
  видит её целой. Иначе бот сообщает «всё пропало» человеку, у которого впереди
  ещё целый день. Пропущенный целиком день обнуляет серию, рекорд остаётся.
- Календарь — квадраты по семь дней с подписью чисел слева (`01–07 🟩⬜…`), а
  не сетка по дням недели: так не нужны пустые клетки в начале месяца, которые
  в переписке выглядят дырой. Рисуется только прожитая часть месяца — будущие
  дни не «пропущены», их ещё не было.
- Динамика произношения молчит, пока попыток меньше четырёх, и сравнивает
  равные половины окна (до десяти попыток в каждой). Одна неудачная фраза при
  сравнении «две против двух» давала бы «−15», и человек решил бы, что стал
  говорить хуже.
- Пустая история показывает приглашение: нули и пустой календарь человек
  читает как поломку, отличить одно от другого ему нечем.
- Дни считаются в местной дате пользователя, той же, по которой сбрасывается
  дневной лимит (`limits.local_today`), иначе серия рвалась бы у всех, кто
  занимается поздно вечером.
- Кнопка «📈 Прогресс» встала во вторую строку меню, рядом с «Профилем» и
  «Подпиской»; раскладка согласована с заказчиком.

**2026-08-22 — Этап 6: Настройки — tag `stage-6-done`** (коммит `f1ef55e`).

Живая проверка человеком в @ChineseToneBot по чек-листу: нижнее меню, смена
уровня, прослушивание и выбор голоса, три шага скорости, смена темы. Всё
работает, настройки переживают перезапуск контейнера — они лежат полями в
`users`, а не в redis.

Решения, которые стоит помнить:

- Голоса живут у провайдера озвучки (`FishTTS.VOICES`), а не в общем конфиге:
  идентификатор принадлежит сервису, и после смены сервиса чужой id нельзя
  показывать выбранным — галочка стояла бы у голоса, которым бот не говорит.
- Шесть голосов отобраны вручную. Верхушка открытого каталога Fish — клоны
  блогеров, ведущих и певцов, они в списке недопустимы. Каждый кандидат прогнан
  живым синтезом «你好，很高兴认识你», слишком торопливые (меньше секунды) и
  тягучие (почти семь) отброшены.
- Смена темы не просто пишет строку: бот сам говорит первую фразу. Для этого
  отдельный промпт `topic_start` (миграция `0009`), приветственный велит
  здороваться, а посреди разговора это выглядит сбоем.
- Образец голоса и первая фраза по теме — платные вызовы, поэтому уходят в arq
  и под замок в redis на 15 секунд: без него десять быстрых тапов дают десять
  оплаченных синтезов.
- Меню — reply-клавиатура, её роутер включён раньше разговорного, иначе надпись
  на кнопке уехала бы в круг как реплика. Раскладка по просьбе заказчика:
  «Общение» на всю первую строку, «Профиль» и «Подписка» на второй. Раздел
  назван у юзера «Профиль», команда осталась `/settings`.

**2026-08-22 — Этап 5: Подписка и оплата — tag `stage-5-done`** (коммит `723d765`).

Проверено живыми деньгами в @ChineseToneBot, а не подставными вебхуками.
**СБП**: заказчик оплатил 549 ₽, вебхук пришёл с `158.160.60.174` и получил
200, платёж `completed` с `paid_at`, подписка до 20.09.2026, уведомление в
боте, `peek` отдал безлимитную квоту, строка остатка исчезла, раздел показал
«Подписка активна до 20.09.2026». **Карта**: два платежа по 549 ₽ с разных
аккаунтов, оба `completed`, у каждого своя подписка. Возврат в бота после
оплаты подтверждён заказчиком.

**Идемпотентность проверена ручной отправкой** настоящего тела вебхука дважды:
оба ответа 200, дата окончания не сдвинулась, два `payment_duplicate`,
уведомление ушло один раз. Вебхук с чужим ключом — 401, видно в access-логе.

По дороге вскрылись и починены три вещи, каждая на живом прогоне: СБП не
умеет рекуррент (сделан разовый тариф `once30` с `PAY2ME/SBP`), платёжка не
даёт продавцу купить у себя (`Incorrect email to purchase` — бот теперь просит
другой адрес и не теряет тариф, добавлена команда `/email`), и возврат с
чарджбэком не закрывали доступ (`refund.success`, `chargeback.initiated`
теперь гасят подписку датой `now()`).

Стек развёрнут досрочно на `104.143.218.104`, домен `chinesetonebot.ru`,
сертификат Let's Encrypt, автовыкатка через GitHub Actions после зелёных
тестов, ночной дамп базы. Подробности — `docs/08_DEPLOY.md`.

**2026-08-15 — Этап 4: Лимиты — tag `stage-4-done`** (коммит `fc70a4b`).

Живая проверка человеком в @ChineseToneBot по чек-листу. Под ответом идёт
строка «🔋 Осталось N из M на сегодня», в пробном периоде с подписью. Стена
исчерпания проверена не выкручиванием тридцати кругов, а правкой
`settings['limits']` на живом боте — это же закрывает критерий «изменение
значения меняет поведение без перезапуска»: следующая реплика упёрлась в стену,
контейнеры не перезапускались.

Замеры и решения:

* **TTL счётчика 43 985 секунд** при замере в 14:46 МСК — ровно до 00:00.
  Обнуляет счётчик сам redis по TTL, отдельной задачи-обнулителя нет.
  `daily_usage` за тот же день показал `(messages=2, checks=0)`.
* **Списание идёт до постановки задачи в очередь.** Платный вызов, сделанный
  «в долг», уже оплачен, отменить его нечем. За стеной в логах воркера пусто —
  это и есть доказательство критерия «платные сервисы не вызываются».
* **Текстовая реплика считается тем же счётчиком, что и голосовая**: круг у них
  один и те же четыре сервиса.
* **Пробный период считается календарными днями.** Регистрация вечером иначе
  съедала бы треть первого дня.
* **Числа читаются из базы на каждой проверке, без кэша.** Кэш превратил бы
  «без перезапуска» в «через N минут».
* **Кнопка «Напомнить завтра» реально напоминает**: заявка ложится в redis,
  почасовая задача воркера сверяет местную дату и зовёт человека. Обещать
  напоминание и не отправлять его — обман, а время у контейнера UTC, поэтому
  «в полночь по расписанию» тут не годится.

Дефект, найденный живой проверкой: **за стеной оставался разбор произношения.**
Счётчики я развёл как полностью независимые, и упёршийся в бесплатный потолок
юзер мог сжечь ещё три вызова SpeechSuper — самой дорогой операции проекта.
Связь сделана односторонней: общая стена закрывает разбор, кончившиеся разборы
разговора не отбирают (так в ТЗ 7.6). Проверка стоит до `INCR`, поэтому
несостоявшийся разбор со своего счётчика не списывается.

Заодно закрыт хвост этапа 2, всплывший на этой же проверке: модель на
бессмысленной реплике сочла, что отвечать нечего, и написала отсутствие ответа
словом — бот озвучил `null` и показал «перевод не сохранился». Отсев пустышек
стоял на всех полях, кроме самого `reply_zh`. Теперь ответ без единого
иероглифа не проходит дальше `_ask_llm`, юзер получает просьбу сказать иначе,
а списанный слот возвращается: за несостоявшийся круг платить дневной нормой
он не должен. Возврат работает и для разбора, когда сервис не расслышал запись.

**2026-08-15 — Этап 3: Оценка произношения — tag `stage-3-done`** (коммит `f488bf5`).

Живая проверка человеком в @ChineseToneBot. Кнопка «Оценка» идёт вторым рядом
под голосовым ответом. Нажатие включает режим «повторите за мной»: эталон
приходит голосом с текстом и пиньинем, следующее голосовое уходит не в
разговорный круг, а на оценку. Семь оценок легли в `pronunciation_checks`
с полным JSON сервиса.

Ключевые замеры:

* Точная фраза `很好！你喜欢什么天气？` — 98 баллов, тоны 96, полнота 100,
  по 100 на каждом из девяти иероглифов (оценка 5).
* Та же фраза со сбитым тоном — 你 получил звуки 100 при балле тона 25
  (оценка 6). Это и есть критерий «сбитый тон роняет балл своего иероглифа»:
  звук чистый, промах именно в тоне.
* Эталон из исправления (оценка 7): юзер сказал `我用算计数`, бот объяснил
  ошибку и отдал `corrected_zh = 我在算数`, эталон синтезирован и озвучен,
  запись получила 80 баллов при полноте 100.

Решения, которые стоит помнить:

* **Тариф `sent.eval.cn`, а не promax.** Ключу заказчика promax не выдан, сервис
  отвергает его с `errId 41030`. Разница одна: promax сообщает услышанный тон
  («нужен 3-й, прозвучал 2-й»), обычный — только балл тона. Разбор к promax
  готов, включается переменной `SPEECHSUPER_CORE_TYPE`.
* **Балл иероглифа — это чистота звуков, а не общая оценка.** На живом прогоне
  слог с полностью перевранным тоном получил по звукам 100. В строке разбора
  всегда сказано, какое число к чему относится, иначе «— 100» читается как
  «всё хорошо».
* **Пиньинь берётся у сервиса** (`symbolpinyin`): он считает сандхи по той же
  фразе, которую разбирал. Локальный `pypinyin` остаётся страховкой.
* **Эталон-реплика бота пересылается по `file_id`**, а не озвучивается заново.
  Синтез нужен только исправленной фразе: её вслух ещё никто не говорил.
* **Режим гаснет после каждой оценки**, иначе юзер, вернувшийся к разговору,
  отправлял бы реплики на проверку произношения. «Ещё раз» включает обратно,
  текстовое сообщение выключает.
* **`corrected_zh` отдельным полем промпта v4.** `correction` объясняет ошибку
  по-русски, произнести его нельзя. Модель и здесь пишет отсутствие значения
  строкой `"null"` — общий отсев слов-пустышек это ловит, на случай эталона
  заведён отдельный тест: иначе бот озвучил бы «повторите за мной: null».

Дефект, найденный проверкой, и **ошибка в его починке**:

1. Пустая запись прошла как речь и дала разбор с баллом 9. Я отсекал только
   полные нули, а живое молчание в микрофон пришло с полнотой 20 — движок
   вытянул подобие фонем из фонового шума.
2. Порог по полноте, поставленный по этому единственному замеру, **отбил вообще
   все живые записи**: настоящая речь давала ту же полноту 20. По цифрам
   молчание и речь неразличимы. Порог убран, отсекается только честный ноль по
   всей фразе, а низкая полнота идёт подсказкой «проговорите фразу целиком»
   рядом с разбором.

Загадка «почему полнота 20» закрыта данными: в тех попытках сервис проставил
нули **разным** слогам, а на другой фразе тот же микрофон дал сплошные сотни —
то есть дело было в самих записях, а не в конвертации. Цепочка
ogg/opus → WAV 16 кГц проверена отдельно: синтезированная речь через неё
получает полноту 100.

**2026-08-14 — Этап 2: Текст и подсказки — tag `stage-2-done`** (коммит `1cd2f61`).

Живая проверка человеком в @ChineseToneBot. Кнопки «Текст» и «Помощь» приходят
вместе с голосовым. «Текст» дописывает разбор в подпись самого голосового
(иероглифы, пиньинь со знаками тонов, перевод) — реплика 40: нажатие 17:49:13,
готово 17:49:16, `пиньинь_локально=False`, в логах воркера за это время ни
одного вызова внешнего сервиса. Это и есть доказательство критерия «без новых
платных вызовов». «Помощь» на той же реплике дала три варианта с пиньинем и
переводом под HSK 1-2, кэш лёг в `dialogs.help_text`.

Решения, которые стоит помнить:

* **Разбор дописывается в подпись голосового, а не приходит сообщением.** Он не
  может разъехаться с ответом, даже если сверху уже прилетели новые реплики, и
  повторное нажатие физически ничего не дублирует.
* **Пиньинь считается ещё и локально** (`pypinyin`, `app/core/services/pinyin.py`).
  Модель уже ломала JSON, и в таком ответе поля `pinyin` нет, а критерий требует
  показывать тоны всегда. Перевод локально взять неоткуда — там честная строка
  «перевод не сохранился», выдумывать его нельзя.
* **Подсказка кэшируется в `dialogs.help_text`** — единственный платный вызов
  этапа, и к одной и той же фразе он не меняется.
* **Контекст для «Помощи» берётся на момент той реплики** (`history_until`), а не
  с текущего конца диалога: кнопку могут нажать под ответом получасовой давности.
* **Разбор словаря модели в `LlmReply` переехал в базовый класс провайдера**,
  реализации отдают сырой JSON (`complete_json`). Иначе каждая новая кнопка
  добавляла бы метод в интерфейс всех провайдеров при том, что отличается
  только промпт.

Два дефекта, найденных **в логах** живой проверки — по экрану их видно не было:

1. **Первое нажатие «Текста» висело 37 секунд** (реплика 34, 17:30:53 → 17:31:31).
   Ответ на нажатие уходил после работы, правка подписи по холодному соединению
   заняла 37 секунд, и запоздалый ответ отлетел с «query is too old»: юзер
   смотрел на крутилку, хендлер падал. Часики теперь гасятся до работы и ровно
   один раз. На соседних репликах то же место занимало 2 секунды — живьём это
   легко списывалось на «интернет тормозит».
2. **Юзеру пришла поправка со словом «null»** (реплика 34): модель написала
   отсутствие ошибки строкой `"null"`, а не JSON-значением. Слова-пустышки
   отсекаются при разборе (`EMPTY_WORDS` в `providers/base.py`), промпт отдельно
   требует значение, а не строку.

Промпт диалога поднят до v3: `correction` обязан объяснить, ЧТО ИМЕННО не так —
как сказал, как правильно, какое правило нарушено. Проверено прогоном боевого
промпта из базы на трёх настоящих китайских ошибках: счётное слово 本 против 个,
是 против 人, лишнее 了 при указании времени — во всех трёх названо правило, а не
только верная фраза. Заодно единообразное «ты»: модель обращалась то так, то так.

Открытый хвост: замок в Redis от двойного тапа живьём не срабатывал ни разу.
Основной механизм — нажатая кнопка исчезает — проверен, замок остаётся
подстраховкой на два тапа за те секунды, пока «Помощь» ещё считается.

**2026-08-14 — Этап 1: Голосовой круг — tag `stage-1-done`** (коммит `95067b1`).

Живая проверка человеком в @ChineseToneBot: `/start` показал кнопки уровня,
после выбора HSK 1-2 бот сам сказал первую фразу голосом (`你好！你好吗？`, 5.9 сек).
Голосовое на русском («Тест, тест-бета») дало ответ по-китайски с пиньинем и
переводом. Голосовое на китайском (3.42 сек) распозналось точно:
`我不太明白你的意思,你能解释一下吗?`, язык chinese. Тихая запись дала «Не расслышал»
вместо выдуманной реплики. Реплики сохраняются в `dialogs`.

Пять дефектов, найденных живой проверкой и починенных:

1. **Whisper выдумывал текст на тишине** — на 2.4 секундах вернул японскую
   заготовку «ご清聴ありがとうございました», и бот всерьёз на неё ответил. Отсев по
   языку, вероятности отсутствия речи и списку типовых заготовок.
2. **Круг не укладывался в норму** — провайдеры пересоздавались на каждый вызов,
   каждый раз новое TLS-рукопожатие. Замеры: холодный круг 10.39 сек, тёплый
   2.47 сек. Общий клиент, кэш провайдеров, прогрев при старте и раз в 4 минуты
   по cron. Первый круг после прогрева — 3.36 сек. Заодно исправлен сам замер:
   отсчёт шёл с распознавания, а скачивание голосового юзер ждал сверх того.
3. **Китайский приходил кириллицей** (`你好` → «Мил!», «Мир холл»). Сначала
   подсказка распознавания была написана по-русски и уводила в кириллицу.
   Первая правка оказалась мёртвой: константу поправили, а литерал в запросе
   остался — и тест это пропустил, потому что сверялся с константой, а не с
   телом запроса. После настоящей правки короткая односекундная фраза всё равно
   не давалась, поэтому сделаны два прохода распознавания (авто и принудительный
   китайский) с арбитражем моделью.
4. **Fish вернул 500** «Inference backend returned empty audio» — круг пропал,
   юзеру пришлось перезаписывать. Обработка сбоя отработала верно, но добавлен
   фолбэк на запасную озвучку (OpenAI). Проверено с битым ключом Fish.
5. **Голос менялся каждую реплику** — без `reference_id` Fish синтезирует новый
   случайный голос, пользователь насчитал трёх собеседников подряд. Задан
   фиксированный нейтральный.

Время круга: сам круг 7-8 сек, скачивание из Telegram на канале разработчика
добавляло 2-8 сек (замерено 1.5-11.6 КБ/с). Критерий итогового времени перенесён
на этап 10, на канал сервера.

Голос выбран заказчиком на слух из четырёх образцов: `温柔叙事女声`
(`23632847285a487d8e0c6ae5bc593c71`), мягкий женский повествовательный.

**2026-08-14 — Этап 0: Каркас — tag `stage-0-done`** (коммит `1c4e4fe`).

Проверка подъёма шла с нуля: образ удалялся, `docker compose up -d` собрал и
поднял `china_bot`, `china_worker`, `china_api` одной командой. Миграции
накатились из entrypoint, в логах ревизия до (пусто) и после (`0001_initial`).
В базе `china_bot` пять таблиц, владелец везде `china_bot`.

Живая проверка человеком: `/start` в @ChineseToneBot. Завёлся пользователь
`5868b141-fb5b-4b9c-a86d-2b432167be14`, identity `telegram:2042819654`
(@Polarasing, Kira), события `user_registered` и `start{is_new_user: true}`.
Повторный `/start` вернул «С возвращением», дубля не создал, событие
`start{is_new_user: false}`. Цепочка логов собралась по одному `request_id`.
`GET /health` — 200, postgres 79 мс, redis 3.3 мс.

Изоляция: чужие базы `landing_bot`/`sbavito`/`mydb` не тронуты, в redis взята
своя база 5 (0 и 3 заняты чужими проектами), единственный ключ — `china:arq:health-check`.

Три бага, найденных и починенных по ходу этапа:
1. **Токен бота утекал в логи.** В маске стоял якорь `` перед цифрами, а в URL
   токен идёт сразу после `bot` (`…/bot<токен>/getMe`) — границы слова там нет.
   Проверено на реальном токене: грепом по логам всех контейнеров его нет.
2. **Сборка падала**: aiogram 3.15.0 требует pydantic < 2.10, был закреплён 2.10.4.
   Понижен до 2.9.2.
3. **`docker compose build` падал** с `image already exists`: три сервиса
   параллельно экспортировали один тег. Образ собирает только `bot`.

Плюс правка логов после первой проверки: поле `user_id` в записи «входящее»
содержало telegram_id, а в остальных записях цепочки — UUID. Разведено.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и **реально проверены человеком**.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
